# PIRS examples

本目录中的 JSON 只用于验证 Schema 和展示字段关系。

- 所有 `example.invalid`、`did:example:*`、摘要、密钥和签名均为虚构占位；
- 不代表真实自然人、组织、Agent、调用、评测、信誉、客户、收入或付款；
- 不授予任何许可、授权、所有权或收益权；
- 不可作为 APC 等级、兼容认证、商业业绩或法律关系的证据；
- 生产实现必须替换全部占位值，并重新生成真实签名、证明和收据。
